apiVersion: v1
kind: Service
metadata:
  name: incident-center-service
  namespace: production
  labels:
    app: incident-center
spec:
  selector:
    app: incident-center
  type: ClusterIP
  ports:
    - name: node
      port: 80
      targetPort: 8080
